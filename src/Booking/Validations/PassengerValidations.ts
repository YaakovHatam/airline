import { Passenger } from "../Models";

export class PassengerValidations {
   static PassengerValidation(passenger: Passenger): boolean {
      return true;
   }

   static PassengerNameValidation(name: string): boolean {
      return true;
   }
}